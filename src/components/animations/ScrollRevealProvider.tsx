"use client";

import React from "react";

// Temporarily disable GSAP animations to fix performance issues
// This component will be re-enabled with proper lazy loading later
export default function ScrollRevealProvider() {
  return null;
}
